import { api } from '@/lib/api';
import { Badge, DataTable, EmptyState, PageShell } from '@novamart/ui';
export default async function RiskPage() { const rows = await (await api()).get<Array<Record<string, unknown>>>('/risk/cases').catch(() => []); return <PageShell eyebrow="Trust & safety" title="Fraud cases" description="Investigate COD, returns, coupons, devices and seller manipulation without changing transactional facts directly."><DataTable><thead><tr><th>Reference</th><th>Category</th><th>Priority</th><th>Status</th><th>Summary</th></tr></thead><tbody>{rows.map((row, index) => <tr key={String(row['id'] ?? index)}><td>{String(row['case_reference'] ?? '—')}</td><td>{String(row['category'] ?? '—')}</td><td><Badge tone={row['priority'] === 'CRITICAL' ? 'danger' : 'warning'}>{String(row['priority'] ?? 'MEDIUM')}</Badge></td><td>{String(row['status'] ?? 'OPEN')}</td><td>{String(row['summary'] ?? '—')}</td></tr>)}</tbody></DataTable>{!rows.length && <EmptyState title="No open cases" description="Risk events that need investigation will create a case trail here." />}</PageShell>; }
