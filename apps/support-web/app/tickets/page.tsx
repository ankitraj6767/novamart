import { api } from '@/lib/api';
import { Badge, DataTable, EmptyState, PageShell } from '@novamart/ui';
export default async function TicketsPage() { const rows = await (await api()).get<Array<Record<string, unknown>>>('/support/tickets').catch(() => []); return <PageShell eyebrow="Support desk" title="Tickets" description="Work from customer context with SLA deadlines and status history maintained by the database."><DataTable><thead><tr><th>Reference</th><th>Subject</th><th>Priority</th><th>Status</th><th>Updated</th></tr></thead><tbody>{rows.map((row, index) => <tr key={String(row['id'] ?? index)}><td>{String(row['ticket_reference'] ?? '—')}</td><td>{String(row['subject'] ?? '—')}</td><td><Badge tone={row['priority'] === 'URGENT' ? 'danger' : 'warning'}>{String(row['priority'] ?? 'NORMAL')}</Badge></td><td>{String(row['status'] ?? 'OPEN')}</td><td>{String(row['updated_at'] ?? '—')}</td></tr>)}</tbody></DataTable>{!rows.length && <EmptyState title="No tickets visible" description="Tickets assigned to you or raised by your account will appear here." />}</PageShell>; }
