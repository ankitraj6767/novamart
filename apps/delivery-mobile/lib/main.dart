import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:go_router/go_router.dart';
void main() => runApp(const ProviderScope(child: DeliveryApp()));
class DeliveryApp extends StatelessWidget { const DeliveryApp({super.key}); @override Widget build(BuildContext context) => MaterialApp.router(title: 'NovaMart Delivery', theme: ThemeData(colorScheme: ColorScheme.fromSeed(seedColor: const Color(0xff3b9a6d)), useMaterial3: true), routerConfig: GoRouter(routes: [GoRoute(path: '/', builder: (_, __) => const DeliveryHome())])); }
class DeliveryHome extends StatelessWidget { const DeliveryHome({super.key}); @override Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Delivery partner')), body: ListView(padding: const EdgeInsets.all(20), children: [Text('Deliver with clarity', style: Theme.of(context).textTheme.displaySmall?.copyWith(fontWeight: FontWeight.w800)), const SizedBox(height: 8), const Text('Assigned routes, OTP verification, proof of delivery and COD reconciliation.'), const SizedBox(height: 24), SwitchListTile(value: true, onChanged: (_) {}, title: const Text('Available for assignments'), subtitle: const Text('Your live availability is shared with operations.')), const Card(child: ListTile(leading: Icon(Icons.route), title: Text('Today’s route'), subtitle: Text('No assignments yet'))), const Card(child: ListTile(leading: Icon(Icons.account_balance_wallet_outlined), title: Text('COD reconciliation'), subtitle: Text('Cash collected and remitted are reconciled in the backend.'))), const SizedBox(height: 12), FilledButton.icon(onPressed: () => context.go('/'), icon: const Icon(Icons.refresh), label: const Text('Refresh assignments'))])); }
