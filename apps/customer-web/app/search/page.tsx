import Link from 'next/link';
import { api } from '@/lib/api';
import { money } from '@novamart/api-client';
import { Card, EmptyState, PageShell } from '@novamart/ui';

type Props = { searchParams: Promise<{ q?: string }> };
export default async function SearchPage({ searchParams }: Props) { const { q = '' } = await searchParams; const result = await (await api()).get<{ items: Array<{ product_id: string; slug: string; title: string; selling_price_paise: string | number; primary_image_url?: string | null }> }>(`/search?q=${encodeURIComponent(q || 'trending')}&limit=24`).catch(() => ({ items: [] })); return <PageShell eyebrow="Search" title={q ? `Results for “${q}”` : 'Explore NovaMart'} description="Typo-tolerant discovery, seller-aware availability and dynamic filters are served by the search boundary."><div className="nm-product-grid">{result.items.map((item) => <Link href={`/product/${item.slug}`} key={item.product_id}><Card className="nm-product-card"><div className="nm-product-image">{item.primary_image_url && <img src={item.primary_image_url} alt="" className="nm-product-image" />}</div><div className="nm-product-copy"><h3>{item.title}</h3><span className="nm-price">{money(item.selling_price_paise)}</span></div></Card></Link>)}</div>{result.items.length === 0 && <EmptyState title="No matching products yet" description="Try a broader search or browse categories from the home page." />}</PageShell>; }
