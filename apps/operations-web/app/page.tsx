import Link from 'next/link';
import { Card, Metric, MetricGrid, PageShell } from '@novamart/ui';
export default function OperationsHome() { const links: Array<[string, string, string]> = [['Shipments','Create labels, ingest tracking and watch delivery exceptions','/shipments'],['Returns','Receive returned units and complete QC','/returns'],['Inventory','Inspect stock, adjustments and reconciliation','/inventory']]; return <PageShell eyebrow="Operations" title="Keep every promise moving" description="Warehouse, shipment, return and inventory work queues in one operations surface."><MetricGrid><Metric label="Shipment queue" value="Live" detail="Carrier events" tone="success" /><Metric label="Returns" value="QC" detail="Inspection workflow" /><Metric label="Inventory" value="Ledger" detail="Reconciliation protected" tone="warning" /><Metric label="SLA" value="Tracked" detail="Promised dates" /></MetricGrid><div className="nm-grid nm-grid-3">{links.map(([title, desc, href]) => <Card key={href}><h2>{title}</h2><p className="nm-muted">{desc}</p><Link className="nm-button nm-button-ghost" href={href}>Open queue →</Link></Card>)}</div></PageShell>; }
