import { api } from '@/lib/api';
import { Badge, DataTable, EmptyState, PageShell } from '@novamart/ui';
export default async function ContentPage() { const rows = await (await api()).get<Array<Record<string, unknown>>>('/admin/platform/home-sections').catch(() => []); return <PageShell eyebrow="Merchandising" title="Homepage CMS" description="Sections are data: scheduled, audience-aware and rendered by every NovaMart client."><DataTable><thead><tr><th>Code</th><th>Type</th><th>Title</th><th>Position</th><th>Status</th></tr></thead><tbody>{rows.map((row, index) => <tr key={String(row['id'] ?? index)}><td>{String(row['code'] ?? '—')}</td><td>{String(row['section_type'] ?? '—')}</td><td>{String(row['title'] ?? '—')}</td><td>{String(row['position'] ?? 0)}</td><td><Badge tone={row['status'] === 'ACTIVE' ? 'success' : 'neutral'}>{String(row['status'] ?? 'DRAFT')}</Badge></td></tr>)}</tbody></DataTable>{!rows.length && <EmptyState title="No homepage sections" description="Create a section from the admin API to start merchandising the storefront." />}</PageShell>; }
